machine:
  services:
    - docker

dependencies:
  override:
    - docker info

test:
  override:
    - make test

# deployment:
#   hub:
#     branch: master
#     commands:
#       - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
#       - docker push circleci/elasticsearch
